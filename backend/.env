PORT=8000
# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-name> with your Atlas credentials
MONGODB_URI=mongodb+srv://snapshare:snapshare123@cluster0.mongodb.net/snapshare?retryWrites=true&w=majority
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# To get your MongoDB Atlas connection string:
# 1. Go to https://cloud.mongodb.com
# 2. Create a free account and cluster
# 3. Create a database user (Database Access)
# 4. Allow network access from anywhere (Network Access -> Add IP -> Allow Access from Anywhere)
# 5. Click Connect -> Connect your application -> Copy the connection string
# 6. Replace the MONGODB_URI above with your connection string
